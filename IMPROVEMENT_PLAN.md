# UnityLocalizationCSVDeepLTranslater 改进计划

## 1. 目标

把当前项目从“可用的个人工具”提升为“安全、可靠、可验证、方便发布和贡献的小型开源工具”。

核心原则：

1. 不泄露用户的 DeepL API Key 或本地化文本。
2. 不静默破坏 Unity Localization CSV、Smart String 或格式占位符。
3. 翻译失败时能够明确报告并安全恢复。
4. 发布产物可追溯、可复现、版本一致。
5. 保持项目轻量，不为小型工具引入没有实际收益的复杂架构。

## 2. 当前基线

- 仓库为公开、未归档状态，默认分支为 `main`。
- 本地 `main` 与 `origin/main` 同步，检查时工作区干净。
- 项目是 Python/Tkinter 外部桌面工具，不是 Unity Package。
- GUI 和翻译核心已经分离，源码可以通过 Python 语法编译检查。
- README 提供中英文说明、截图、快速开始、费用与 API Key 警告。
- 当前没有自动测试、GitHub Actions、明确的开源许可证或社区治理文件。
- `v1.0.0` 标签与程序内 `v1.1` 标题不一致。
- 仓库直接跟踪约 16.4 MB 的 Windows EXE，当前文件没有数字签名。

## 3. 优先级路线图

### 阶段 0：安全与开源合法性

目标：先消除最直接的密钥风险，并明确项目的开源授权。

#### 0.1 修复 API Key 存储与忽略规则

- [x] 在 `.gitignore` 中至少增加根目录 `/config.ini`。
- [x] 将配置、输入和输出目录解析为明确的应用数据路径，不依赖启动时的当前工作目录。
- [x] 优先使用系统凭据存储；若暂时保留明文配置，记录安全限制并尽量限制文件权限。
- [x] 确认日志、异常和 UI 不会显示完整 API Key。
- [x] 对当前 Git 历史执行一次不会输出密钥内容的 secret scan。

验收标准：

- 从源码和打包程序启动时，都不会在仓库内生成可被 Git 跟踪的密钥文件。
- 自动检查能够阻止 `config.ini` 或疑似凭据进入提交。
- README 清楚说明 Key 保存位置和撤销方式。

#### 0.2 添加开源许可证

- [x] 根据作者意图选择许可证，轻量宽松授权优先考虑 MIT 或 Apache-2.0。
- [x] 添加根目录 `LICENSE`。
- [x] 在 README 中显示许可证，并确认依赖和打包方式与所选许可证兼容。

验收标准：GitHub 能识别许可证，使用者能明确知道复制、修改和再分发条件。

### 阶段 1：翻译结果完整性

目标：任何翻译都不能静默损坏 Unity 文本结构。

#### 1.1 重做占位符和标签保护

- [x] 定义需要支持的格式：Python/.NET 格式符、printf、Unity Smart Strings、ICU、转义花括号、富文本标签和换行。
- [x] 用可靠的扫描器或解析器代替当前非嵌套正则方案。
- [x] 翻译前提取占位符和标签的多重集合。
- [x] 翻译后验证集合、数量及必要的嵌套结构。
- [x] 验证失败时保留原单元格、记录明确错误，不写入损坏译文。

验收标准：测试覆盖 `%1$s`、`{{literal}}`、嵌套复数 Smart String、富文本标签和重复占位符，全部能够安全往返。

#### 1.2 强化 CSV schema 和往返处理

- [x] 验证 `Key`、`Id`、源语言列和目标语言列。
- [x] 拒绝空表头、重复表头和字段数量异常的行。
- [x] 明确支持的 Unity Localization 导出格式与已测试版本。
- [x] 保留 BOM、列顺序、行顺序、嵌入逗号、引号和多行文本。
- [x] 为不支持的语言列提供明确提示，而不是静默忽略。

验收标准：合法 CSV 导入 Unity 前后结构一致；非法文件在调用 DeepL 前就被拒绝并给出文件级诊断。

#### 1.3 原子输出和部分失败策略

- [x] 先写同目录临时文件，成功刷新和关闭后再原子替换最终输出。
- [x] 文件失败时清理临时产物，不覆盖上一次成功结果。
- [x] 定义 `success`、`partial`、`failed` 三种结果状态。
- [x] 输出失败单元格清单或可重试报告。
- [x] GUI 明确显示部分成功，不能只显示“处理完成”。

验收标准：模拟写入失败、翻译失败和应用退出时，已有输出不会损坏，用户能识别哪些内容未完成。

### 阶段 2：测试与持续集成

目标：核心行为可以在不产生 DeepL 费用的情况下重复验证。

#### 2.1 建立测试基础

- [ ] 添加 `pytest` 测试目录和 fake DeepL client。
- [ ] 为 CSV、schema、跳过规则、占位符、缓存、异常分类和统计编写单元测试。
- [ ] 添加端到端临时目录测试，验证输入到输出的完整流程。
- [ ] 所有自动测试禁止访问真实 DeepL API。

最低测试矩阵：

- UTF-8 与 UTF-8 BOM。
- 逗号、双引号和多行字段。
- 空白、重复、额外和缺失字段。
- 已有译文保护与覆盖模式。
- Smart String、ICU、printf 和富文本。
- 超时、429、认证失败、额度不足和部分失败。
- 原子写入失败后的恢复。

#### 2.2 添加 GitHub Actions

- [ ] 在 Windows 和 Linux 上运行语法检查与测试。
- [ ] 覆盖项目声明支持的 Python 版本。
- [ ] 加入依赖缓存，但不缓存用户数据或密钥。
- [ ] 在 PR 和 `main` push 时运行。
- [ ] 可选：增加静态检查、格式检查和 secret scanning。

验收标准：PR 必须在零真实 API 调用的情况下通过全部检查。

### 阶段 3：DeepL 调用效率与用户控制

目标：降低大批量任务的等待时间和不可控性。

#### 3.1 分类错误和重试

- [ ] 只对网络临时错误、服务端错误和限流进行重试。
- [ ] 认证、参数和额度错误立即失败。
- [ ] 遵循 `Retry-After` 或 SDK 的官方重试建议。
- [ ] 最后一次失败后不再休眠。
- [ ] 对用户显示可操作的错误分类，避免直接暴露冗长内部异常。

#### 3.2 批量翻译、缓存和用量预估

- [ ] 按 DeepL 限制批量提交文本，而不是每个单元格单独请求。
- [ ] 在同一次任务内跨文件缓存相同的源文本/目标语言组合。
- [ ] 翻译前显示预计字符数和目标语言数量。
- [ ] 支持取消长任务，并确保取消时安全完成或回滚当前文件。
- [ ] 评估是否需要可选的持久缓存；默认不得泄露本地化内容。

验收标准：相同输入不产生重复请求，大表的请求数显著下降，取消操作不会留下损坏文件。

### 阶段 4：UI、文档与开发体验

目标：让 README、界面和实际功能保持一致，并降低贡献门槛。

#### 4.1 修正文档与产品声明

- [ ] 修正“可以选择源语言和目标语言”的描述，或实际实现对应选择器。
- [ ] 将“跨平台”改为“源码支持的平台”和“预编译包支持的平台”两部分。
- [ ] 用“已测试兼容”代替“完美适配”，列出 Unity Localization 版本和已知限制。
- [ ] 添加源码安装、虚拟环境、Python 版本、运行、测试和构建说明。
- [ ] 补充隐私说明：CSV 文本会发送给 DeepL，工具本身不应增加其他上传渠道。

#### 4.2 整理 Python 工程元数据

- [ ] 移除未使用的 `pandas`。
- [ ] 声明 Python 3.10+ 和合理的依赖版本范围。
- [ ] 添加 `pyproject.toml`，集中管理项目、测试和工具配置。
- [ ] 评估中英文 UI；若不实现语言切换，README 应清楚说明当前 UI 语言。

#### 4.3 社区健康文件

- [ ] 添加 `CONTRIBUTING.md`。
- [ ] 添加 `SECURITY.md`，说明密钥或漏洞的非公开报告方式。
- [ ] 添加 Bug/功能请求模板和 PR 模板。
- [ ] 添加 `CHANGELOG.md`。

### 阶段 5：可复现发布

目标：源码、版本、构建产物和 GitHub Release 完全对应。

- [ ] 建立单一版本来源，并统一窗口标题、Git tag、Release 名称和文件名。
- [ ] 提交并维护 PyInstaller spec 或等价的可复现构建配置。
- [ ] 从源码仓库移除后续 EXE 更新，二进制改为 GitHub Release asset。
- [ ] 在干净环境或 CI 中构建 Windows 产物。
- [ ] 每个产物发布 SHA-256 校验文件。
- [ ] 有条件时使用 Authenticode 代码签名。
- [ ] 使用语义化版本和有意义的提交/发布说明。
- [ ] 发布前运行完整测试，并用真实但非敏感的示例 CSV 做人工往返验证。

验收标准：任一 Release 都能从对应 tag 重建，版本信息一致，用户可验证下载文件完整性。

## 4. 推荐实施批次

### 批次 A：立即修复

范围：阶段 0，以及阶段 1.1 的占位符完整性保护。

建议版本：`v1.1.1` 或下一个补丁版本。此批次不加入大型 UI 功能，重点消除密钥和数据损坏风险。

### 批次 B：可靠性基础

范围：阶段 1.2、1.3 和阶段 2。

建议版本：`v1.2.0`。完成后项目应具备可持续修改的测试安全网。

### 批次 C：效率与体验

范围：阶段 3 和阶段 4。

建议版本：`v1.3.0`。在已有测试保护后再调整批量请求、取消机制和 UI。

### 批次 D：规范发布

范围：阶段 5，以及剩余社区文件。

此批次可以与 `v1.2.0` 或 `v1.3.0` 合并执行，但不应早于基本测试和版本统一。

## 5. 首个实施 PR 建议

为了控制改动风险，第一个 PR 建议只包含：

1. 添加 `LICENSE`（许可证需作者确认）。
2. 修复根目录 `config.ini` 忽略规则。
3. 引入安全的应用配置路径；若暂不接入 keyring，清楚记录明文存储限制。
4. 添加最小 `pytest` 框架和 fake DeepL client。
5. 为当前占位符问题加入失败测试，先固定预期，再实现解析与完整性校验。
6. 添加最小 GitHub Actions workflow。
7. 更新 README 的安全说明和源码运行方式。

首个 PR 的完成定义：

- 不包含任何真实 API Key 或用户 CSV。
- 语法检查和全部测试通过。
- 测试期间没有外部 DeepL 请求。
- 根目录和打包程序目录都不会生成可被 Git 跟踪的密钥文件。
- 已知占位符格式不会被静默损坏。
- README 与实际行为一致。

## 6. 暂不建议优先做的事项

- 暂不将项目重写为 Unity Editor 插件或 UPM Package；这会改变产品定位和维护成本。
- 暂不引入数据库、Web 服务或复杂插件系统。
- 暂不为了优化旧提交信息而重写已经发布的 Git 历史。
- 在测试和数据完整性问题解决前，暂不优先扩展更多翻译供应商。

## 7. 决策待确认

实施前需要作者明确以下选择：

1. 开源许可证采用 MIT、Apache-2.0、GPL，还是其他许可证。
2. 是否接受新增 `keyring` 依赖，使用系统凭据存储 API Key。
3. 源语言是否继续固定为英语，还是正式实现源/目标语言选择器。
4. 需要保证兼容的 Unity Localization 版本范围。
5. 是否保留仓库历史中的 EXE，还是仅从后续版本开始改用 Release assets。
