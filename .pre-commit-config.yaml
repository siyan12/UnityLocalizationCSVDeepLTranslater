repos:
  - repo: local
    hooks:
      - id: credential-safety
        name: Reject config.ini and likely credentials
        entry: python scripts/check_secrets.py --staged
        language: system
        pass_filenames: false
